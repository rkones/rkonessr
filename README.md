# rkonessr

## Countdown Widget

Run `Countdown.ps1` in PowerShell to display a desktop countdown widget.

Example:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\Countdown.ps1
```

The widget stays on top and updates every second.
